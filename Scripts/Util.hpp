#pragma once

#include <vector>
#include <numeric>
#include <cmath>

double vectorMean(std::vector<double>&);
double vectorMean(std::vector<int>&);

double vectorVar(std::vector<double>&);
double vectorVar(std::vector<int>&);